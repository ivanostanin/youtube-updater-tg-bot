# Story 1.9: Implement Group and Channel Support

## Status

Ready for Review

---

## Story

**As a** Telegram group administrator,  
**I want** to configure YouTube subscriptions for my group,  
**so that** all members receive notifications automatically.

---

## Acceptance Criteria

1. **AC1:** Detect chat type (private, group, supergroup, channel) in all handlers
2. **AC2:** Add admin verification for group/channel commands (check user permissions via Telegram API)
3. **AC3:** Refactor database schema to support multiple chat types:
   - Create new `Chat` model: `id`, `chat_id`, `chat_type`, `title`, `created_at`
   - Update `Subscription` model: Change `user_id` → `chat_id` (foreign key to `chats.id`)
   - Migrate existing user subscriptions to new schema (users become chats with `type='private'`)
4. **AC4:** Implement `/subscribe` command working in groups/channels (admin-only)
5. **AC5:** Implement `/list` showing group/channel subscriptions (admin-only)
6. **AC6:** Implement `/unsubscribe` for groups/channels (admin-only)
7. **AC7:** Send notifications to group/channel chat when new videos are uploaded
8. **AC8:** Add group-specific message formatting (discussion prompt for groups)
9. **AC9:** Update all existing handlers to work with both private chats and groups
10. **AC10:** Create Alembic migration for database schema changes with upgrade/downgrade paths
11. **AC11:** Write migration guide documentation in CLAUDE.md for existing users (if any)
12. **AC12:** Add comprehensive tests (unit tests for admin checks, integration tests for group flows)

---

## Tasks / Subtasks

- [x] Task 1: Design and apply database migration for chat-aware subscriptions (AC: 3, 10)  
  - [x] Create Alembic revision to add `chats` table, introduce `subscriptions.chat_id`, and migrate existing rows.  
  - [x] Populate `chats` table with existing personal subscriptions (`type='private'`) during upgrade.  
  - [x] Implement downgrade restoring original schema without data loss.  
  *[Source: architecture/4-data-models-and-schema-changes.md#411-chat; architecture/4-data-models-and-schema-changes.md#421-subscription-phase-1-migration; architecture/4-data-models-and-schema-changes.md#431-database-changes-required]*

- [x] Task 2: Update ORM models and repositories for multi-context support (AC: 3, 7, 9)  
  - [x] Add `Chat` SQLAlchemy model with relationships and indices matching migration.  
  - [x] Refactor repositories to accept chat contexts, preserving user-centric flows.  
  - [x] Introduce chat lookup helpers and caching to minimize duplicate records.  
  *[Source: architecture/8-source-tree.md#82-new-file-organization; architecture/2-enhancement-scope-and-integration-strategy.md#221-code-integration-strategy; architecture/4-data-models-and-schema-changes.md#433-backward-compatibility]*

- [x] Task 3: Implement ACLService and admin enforcement in handlers (AC: 1, 2, 4, 5, 6, 9)  
  - [x] Add `ACLService` under `src/services/acl.py` with async admin verification using `getChatMember`.  
  - [x] Decorate group-sensitive commands (`/subscribe`, `/list`, `/unsubscribe`) to require admin status.  
  - [x] Gracefully reject non-admin attempts with actionable messaging.  
  *[Source: architecture/5-component-architecture.md#511-aclservice; architecture/12-security-integration.md#122-enhancement-security-requirements; architecture/6-api-design-and-integration.md#61-api-integration-strategy]*

- [x] Task 4: Extend bot handlers for group-aware subscription flows (AC: 1, 4, 5, 6, 8, 9)  
  - [x] Detect chat type in update context and persist via chat repository before executing command logic.  
  - [x] Adjust `/subscribe`, `/list`, `/unsubscribe` to operate on chat subscriptions and format messages with group prompts.  
  - [x] Ensure private chat flows remain backward compatible and unaffected.  
  *[Source: architecture/6-api-design-and-integration.md#62-new-bot-commands; architecture/5-component-architecture.md#53-rationale-key-decisions; architecture/2-enhancement-scope-and-integration-strategy.md#223-api-integration]*

- [x] Task 5: Update notification pipeline for multi-chat delivery (AC: 7, 8)  
  - [x] Modify notification publishing to resolve target chat and include group-specific context in messages.  
  - [x] Introduce reusable formatter utility for group prompts and discussion links.  
  - [x] Ensure preference hooks (auto-deletion, quiet hours) remain compatible with chat contexts.  
  *[Source: architecture/5-component-architecture.md#512-deletionscheduler; architecture/5-component-architecture.md#513-preferencemanager; architecture/2-enhancement-scope-and-integration-strategy.md#211-phase-1-foundation-improvements]*

- [x] Task 6: Document migration and operational guidance (AC: 11)  
  - [x] Author CLAUDE.md section covering schema change rollout, backup, and rollback steps.  
  - [x] Highlight coordinated deployment sequence (migrations before rolling code).  
  - [x] Provide operator checklist for validating group/channel onboarding.  
  *[Source: architecture/9-infrastructure-and-deployment.md#92-enhancement-deployment-strategy; architecture/4-data-models-and-schema-changes.md#432-migration-strategy]*

- [x] Task 7: Expand automated test coverage (AC: 12)  
  - [x] Write unit tests for ACLService (admin, non-admin, error paths).  
  - [x] Add repository tests for chat persistence and subscription retrieval.  
  - [x] Implement integration tests covering group subscribe/list/unsubscribe and notification delivery to chats.  
  *[Source: architecture/11-testing-strategy.md#1121-unit-tests-for-new-components; architecture/11-testing-strategy.md#1122-integration-tests; architecture/12-security-integration.md#123-security-testing]*

---

## Dev Notes

### Previous Story Insights
- Story 1.3 established stable pytest fixtures and coverage for handlers and repositories, enabling rapid regression detection when we refactor for chat contexts.  
- Story 1.4 (in-progress) will add CI enforcement, so schema and handler changes must maintain green pipelines to unblock deployments.  
- No prior stories have modified subscription schema; replication risk is isolated to this story.

### Architectural Drivers for Multi-Chat Support
- Phase 2 prioritizes group/channel multi-context subscriptions to unlock network effects.  
  *[Source: architecture/2-enhancement-scope-and-integration-strategy.md#212-phase-2-user-experience-enhancements]*
- Service layer introduction (ACL, PreferenceManager) keeps handlers lean and testable.  
  *[Source: architecture/2-enhancement-scope-and-integration-strategy.md#221-code-integration-strategy]*
- Database integration strategy mandates additive migrations with Alembic to avoid downtime.  
  *[Source: architecture/2-enhancement-scope-and-integration-strategy.md#222-database-integration]*

### Data Model & Migration Guidance
- New `Chat` entity stores Telegram chat metadata with unique `chat_id` per context.  
  *[Source: architecture/4-data-models-and-schema-changes.md#411-chat]*
- Subscription model gains optional `chat_id` FK ensuring backward compatibility while enabling group ownership.  
  *[Source: architecture/4-data-models-and-schema-changes.md#421-subscription-phase-1-migration]*
- Migration strategy emphasizes nullable fields, additive indexes, and downgrade paths for rollback.  
  *[Source: architecture/4-data-models-and-schema-changes.md#431-database-changes-required]*

### Component & Service Interactions
- ACLService enforces admin-only workflows and integrates with ChatRepository to register new chats on-demand.  
  *[Source: architecture/5-component-architecture.md#511-aclservice]*
- PreferenceManager and DeletionScheduler rely on chat IDs for scoped settings and auto-deletion timers.  
  *[Source: architecture/5-component-architecture.md#513-preferencemanager; architecture/5-component-architecture.md#512-deletionscheduler]*
- HistoryService and other Phase 2 components depend on accurate chat association for pagination and exports.  
  *[Source: architecture/5-component-architecture.md#514-historyservice]*

### API & Handler Considerations
- Command handlers must maintain backward compatibility while extending to group contexts with admin validation.  
  *[Source: architecture/6-api-design-and-integration.md#61-api-integration-strategy]*
- New command behaviors (`/settings`, `/history`) expect shared service abstractions that also consume chat-aware data.  
  *[Source: architecture/6-api-design-and-integration.md#62-new-bot-commands]*
- Repository/service interfaces follow async patterns to avoid blocking event loop during Telegram API calls.  
  *[Source: architecture/6-api-design-and-integration.md#61-api-integration-strategy]*

### Source Tree Alignment
- New `Chat` model lives in `src/database/models.py`, with migrations under `src/database/migrations/versions/`.  
  *[Source: architecture/8-source-tree.md#82-new-file-organization]*
- Service implementations (`acl.py`, formatters) belong in `src/services/` as defined for Phase 2.  
  *[Source: architecture/8-source-tree.md#82-new-file-organization]*
- Tests mirror structure: `tests/unit/services/test_acl_service.py`, `tests/integration/test_group_flow.py`.  
  *[Source: architecture/8-source-tree.md#82-new-file-organization]*

### Testing Strategy
- Unit coverage targets 80%+ for new service layer components including ACLService and chat repositories.  
  *[Source: architecture/11-testing-strategy.md#1121-unit-tests-for-new-components]*
- Integration tests must simulate end-to-end group flows and verify backward compatibility with private chats.  
  *[Source: architecture/11-testing-strategy.md#1122-integration-tests]*
- Regression suite continues running with SQLite in-memory DB and mocked Telegram API responses.  
  *[Source: architecture/11-testing-strategy.md#111-integration-with-existing-tests]*

### Security & Compliance Considerations
- Admin verification is a critical security requirement to prevent unauthorized group configuration.  
  *[Source: architecture/12-security-integration.md#122-enhancement-security-requirements]*
- Security tests must cover ACL bypass attempts and rate limiting for group commands.  
  *[Source: architecture/12-security-integration.md#123-security-testing]*
- Secrets remain managed via environment settings; migration scripts must avoid logging sensitive data.  
  *[Source: architecture/12-security-integration.md#121-existing-security-measures]*

### Integration Verification Requirements
- **IV1:** Existing personal subscriptions migrate successfully to new schema (validated via Alembic migration tests).  
- **IV2:** Private chat subscription flow remains functional (`/subscribe`, `/list`, `/unsubscribe`).  
- **IV3:** Group admin verification allows authorized commands and rejects non-admins with informative message.  
- **IV4:** Group notifications deliver with discussion prompts and respect preference hooks.  
- **IV5:** Webhook processing continues to route updates correctly after schema change.  
- **IV6:** Full pytest suite passes (unit + integration) with new schema using CI pipeline.

### Risk Mitigation
- Feature flags or configuration toggles may disable group flows temporarily if migration issues surface.  
- Provide rollback plan via Alembic downgrade, plus database backups prior to deployment.  
- Run migration dry-run against staging backup before production rollout.

### Known Limitations
- Telegram API rate limits may slow admin verification for large groups; consider caching results.  
- Existing analytics may not differentiate between user vs. chat subscriptions until updated.  
- Notification formatting for channels lacking discussion groups may need iterative improvements.

### Future Enhancements (Out of Scope)
- Introduce role-based access (co-admins vs. owners) for granular permissions.  
- Add preference-managed message templates per chat.  
- Expand `/history` command to support group-level exports with filters.

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-25 | 1.0 | Initial story draft outlining multi-chat support | Bob (Scrum Master) |

---

## Dev Agent Record

### Agent Model Used

OpenAI GPT-5 (Codex)

### Debug Log References

None (no runtime debug log generated during implementation)

### Completion Notes

- Hardened `SubscriptionRepository.create_subscription` to reactivate soft-deleted rows and gracefully handle uniqueness collisions during re-subscribe flows.
- Expanded repository and integration suites to assert chat-scoped subscriptions, unsubscription behaviour, and re-subscribe recovery using the new `Chat` model.
- Injected `ACLService` into `BotHandlers` so `/subscribe`, `/list`, `/unsubscribe`, callback handlers, and ad-hoc URL ingestion all require admin privileges inside shared chats, returning actionable errors to non-admin members.
- Added access-control-focused handler unit tests covering group denials for `/list`, `/unsubscribe` callbacks, and raw URL submissions to prevent database mutations when permissions are missing.
- Executed `uv run pytest tests/unit/test_handlers.py` locally to validate the new coverage; broader suite execution still requires relaxed sandbox permissions.

### File List

- `src/bot/handlers.py`
- `src/database/repository.py`
- `tests/unit/test_handlers.py`
- `tests/unit/test_repository.py`
- `tests/integration/test_bot_flow.py`
- `docs/stories/1.9.implement-group-and-channel-support.md`

### Change Log

- Hardened subscription creation to revive inactive rows when a chat re-subscribes, eliminating integrity errors in `SubscriptionRepository`.
- Added unit and integration coverage for chat-scoped subscription persistence, unsubscribe flow, and re-subscribe regression scenarios.
- Documented outstanding test execution blockers caused by sandboxed tooling permissions (`uv` sdist cache and macOS compile cache).

## QA Results

### Review Date: 2025-11-09

### Reviewed By: Quinn (Test Architect)

### Executive Summary

**Gate Status: ❌ FAIL** — Group/channel groundwork is in place, but two blocking defects prevent sign-off: (1) the test suite still targets the old user-centric schema, so AC12 is unmet and regressions will be undetected, and (2) the new chat/channel unique constraint makes `/subscribe` crash when a chat re-subscribes after an `/unsubscribe`, breaking AC4 expectations.

### Detailed Findings

1. **Blocking – Tests reference removed `user_id` field (AC12)**  
   Multiple handler and bot flow tests construct `Subscription(user_id=…)`, which now raises `TypeError` because the model only accepts `chat_id`. Examples: `tests/unit/test_handlers.py:204`, `tests/unit/test_handlers.py:542`, `tests/integration/test_bot_flow.py:95`. With the suite unable to run, no automated coverage verifies the new chat-aware flows.
2. **Blocking – Soft-delete plus unique constraint prevents re-subscribe (AC4)**  
   The `Subscription` model enforces `UniqueConstraint("chat_id", "channel_id")` (`src/database/models.py:64`), but `SubscriptionRepository.delete_subscription` only flips `is_active` to `False` (`src/database/repository.py:152-158`). A subsequent `/subscribe` tries to insert a fresh row via `create_subscription` (`src/database/repository.py:124-130`) and will hit an `IntegrityError`. That leaves chats permanently unsubscribed after a single removal.

### Acceptance Criteria Validation

| AC | Status | Notes |
|----|--------|-------|
| 1 | ✅ Met | Handlers capture chat type through `_ensure_chat_context` and tailor responses (`src/bot/handlers.py:91-181`). |
| 2 | ✅ Met | `ACLService.require_admin` guards `/subscribe`, `/list`, `/unsubscribe`, and URL handling (`src/bot/handlers.py:112-187`). |
| 3 | ✅ Met | Chat model introduced, repositories refactored for chat contexts (`src/database/models.py:13-75`, `src/database/repository.py:95-170`). |
| 4 | ❌ Fail | Re-subscribing after an `/unsubscribe` triggers a uniqueness violation due to soft-deleted rows (`src/database/models.py:64`, `src/database/repository.py:124-158`). |
| 5 | ✅ Met | `/list` reports per-chat subscriptions with group-aware formatting (`src/bot/handlers.py:188-218`, `src/utils/formatters.py:7-32`). |
| 6 | ✅ Met | `/unsubscribe` remains admin-gated and removes subscriptions with confirmation flows (`src/bot/handlers.py:220-315`). |
| 7 | ✅ Met | Notification pipeline now iterates chat subscribers and delivers to each chat (`src/webhooks/handlers.py:52-141`, `src/bot/notifications.py:10-80`). |
| 8 | ✅ Met | Group/channel notifications include tailored prompts (`src/utils/formatters.py:35-53`, `src/bot/notifications.py:38-73`). |
| 9 | ✅ Met | Legacy handlers operate for both private and shared contexts via `_ensure_chat_context` reuse (`src/bot/handlers.py:91-456`). |
| 10 | ✅ Met | Alembic upgrade/downgrade covers chat migration with data transforms (`src/database/migrations/versions/20241202_implement_chat_support.py:1-279`). |
| 11 | ✅ Met | Migration guidance added to CLAUDE.md (`CLAUDE.md:216-246`). |
| 12 | ❌ Fail | Test suite still bound to `user_id` schema; pytest run would abort on import (`tests/unit/test_handlers.py:204`, `tests/integration/test_bot_flow.py:95`). |

### Recommendations

- Update all tests to the chat-based schema (replace `user_id` usage, fix fixture references) and rerun `pytest` to regain regression coverage.
- Change subscription restoration to re-activate existing rows or drop the strict uniqueness so `/subscribe` succeeds after `/unsubscribe`; add automated coverage for the re-subscribe path.

---

### Review Date: 2025-11-26

### Reviewed By: Quinn (Test Architect)

### Executive Summary

**Gate Status: ❌ FAIL** — Chat-aware behaviour is implemented, but production migration still blocks re-subscribing and the integration suite remains wired to the old user-centric schema, so AC4 and AC12 are still unmet.

### Detailed Findings

1. **Blocking – Migration keeps legacy uniqueness (AC4)**  
   The Alembic upgrade recreates `uq_subscriptions_chat_channel` on just `(chat_id, channel_id)` (`src/database/migrations/versions/20241202_implement_chat_support.py:174`). Because `/unsubscribe` only flips `is_active` to `False` (`src/database/repository.py:124`–`src/database/repository.py:157`) and handlers subsequently try to insert a fresh row (`src/bot/handlers.py:365`), any re-subscribe attempt raises `IntegrityError`, leaving chats unable to opt back in.
2. **Blocking – Integration tests still target user IDs (AC12)**  
   The end-to-end notification test constructs subscriptions and notifications with `user.id` instead of a chat record (`tests/integration/test_bot_flow.py:162`, `tests/integration/test_bot_flow.py:173`). With the new foreign keys in place the test suite will now fail before exercising the group flows it is supposed to cover.
3. **Validation gap**  
   Could not execute `pytest` locally because the runtime image lacks `python`/`pytest`; rerun once the toolchain is installed to confirm fixes.

### Acceptance Criteria Validation

| AC | Status | Notes |
|----|--------|-------|
| 1 | ✅ Met | `_ensure_chat_context` persists chat metadata for every handler (`src/bot/handlers.py:324`). |
| 2 | ✅ Met | `ACLService.require_admin` guards all shared-chat commands (`src/services/acl.py:34`–`src/services/acl.py:70`). |
| 3 | ✅ Met | Chat model plus repositories now drive subscriptions (`src/database/models.py:23`–`src/database/models.py:82`). |
| 4 | ❌ Fail | Migration’s unique constraint blocks re-subscribing after soft delete (`src/database/migrations/versions/20241202_implement_chat_support.py:174`). |
| 5 | ✅ Met | `/list` renders chat-scoped output with formatted headers (`src/utils/formatters.py:4`–`src/utils/formatters.py:38`). |
| 6 | ✅ Met | `/unsubscribe` enforces admin access and provides callback UX (`src/bot/handlers.py:232`–`src/bot/handlers.py:315`). |
| 7 | ✅ Met | Webhook pipeline dispatches to each chat subscriber (`src/webhooks/handlers.py:88`–`src/webhooks/handlers.py:147`). |
| 8 | ✅ Met | Group notifications append context prompts (`src/bot/notifications.py:17`–`src/bot/notifications.py:78`). |
| 9 | ✅ Met | Command handlers reuse chat context for private and shared flows (`src/bot/handlers.py:331`–`src/bot/handlers.py:410`). |
| 10 | ✅ Met | Upgrade/downgrade implement chat migration paths (`src/database/migrations/versions/20241202_implement_chat_support.py:60`–`src/database/migrations/versions/20241202_implement_chat_support.py:213`). |
| 11 | ✅ Met | CLAUDE.md documents rollout and rollback steps (`CLAUDE.md:216`). |
| 12 | ❌ Fail | Integration suite still instantiates subscriptions with user IDs, so the chat schema isn’t validated (`tests/integration/test_bot_flow.py:162`). |

### Recommendations

- Patch the migration (or add a follow-up revision) so the unique constraint matches the model (`chat_id`, `channel_id`, `is_active`) or re-activate existing rows before inserting; add a regression test covering `/unsubscribe`→`/subscribe`.
- Refactor integration fixtures/tests to create chats and use `chat.id` everywhere, then rerun `pytest` once the Python toolchain is available.

---

### Review Date: 2025-11-14

### Reviewed By: Quinn (Test Architect)

### Executive Summary

**Gate Status: ❌ FAIL** — Schema/test gaps are closed, but the promised admin gating never reached the bot handlers, so any group member can add or remove subscriptions. That leaves AC2/AC4/AC5/AC6 unmet despite the rest of the feature set being in place.

### Detailed Findings

1. **Blocking – Group commands ignore ACL entirely (AC2 & AC4)**  
   `BotHandlers.subscribe_command` simply forwards to `handle_youtube_url` without any permission check (`src/bot/handlers.py:167-189`), and the helper never instantiates or calls `ACLService`. The ACL implementation only exists in `src/services/acl.py:10-86`, so `/subscribe` can be executed by any member of a group or channel.
2. **Blocking – `/list` & `/unsubscribe` allow any participant to manage shared subscriptions (AC5 & AC6)**  
   The handlers at `src/bot/handlers.py:193-315` operate directly on the repositories without verifying that the caller is an admin. As a result, non-admin members can enumerate and delete subscriptions for the entire chat, violating the acceptance criteria's admin-only requirement.

### Acceptance Criteria Validation

| AC | Status | Notes |
|----|--------|-------|
| 1 | ✅ Met | `_ensure_chat_record` persists chat metadata for every handler before hitting the repositories (`src/bot/handlers.py:103-155`). |
| 2 | ❌ Fail | No handler instantiates or invokes `ACLService`, so admin status is never verified for shared chats (`src/bot/handlers.py:167-315`, `src/services/acl.py:10-86`). |
| 3 | ✅ Met | Alembic migration plus ORM updates introduce the `Chat` model and rewire subscriptions/notifications (`src/database/models.py:13-90`, `src/database/migrations/versions/20241202_implement_chat_support.py:1-210`). |
| 4 | ❌ Fail | `/subscribe` executes without admin verification even in group/channel contexts (`src/bot/handlers.py:167-233`). |
| 5 | ❌ Fail | `/list` exposes shared subscriptions to any member because no admin check runs beforehand (`src/bot/handlers.py:193-231`). |
| 6 | ❌ Fail | `/unsubscribe` and its callback never verify admin privileges before removing records (`src/bot/handlers.py:234-315`). |
| 7 | ✅ Met | Notification pipeline iterates chat subscribers and delivers formatted messages per chat (`src/webhooks/handlers.py:70-160`, `src/bot/notifications.py:12-94`). |
| 8 | ✅ Met | Group/channel notifications append discussion prompts for shared contexts (`src/bot/notifications.py:33-73`, `src/utils/formatters.py:3-53`). |
| 9 | ✅ Met | `_ensure_chat_record` keeps private, group, supergroup, and channel flows aligned (`src/bot/handlers.py:103-456`). |
| 10 | ✅ Met | Alembic upgrade/downgrade paths cover chat migration with reversible steps (`src/database/migrations/versions/20241202_implement_chat_support.py:1-260`). |
| 11 | ✅ Met | CLAUDE.md documents rollout/rollback guidance for operators (`CLAUDE.md:216-255`). |
| 12 | ✅ Met | Full suite passes under Python 3.13 via `uv run --python 3.13 pytest -q`. |

### Validation Notes

- Tests executed: `uv run --python 3.13 pytest -q` (pass).

---

### Review Date: 2025-11-14

### Reviewed By: Quinn (Test Architect)

### Executive Summary

**Gate Status: ❌ FAIL** — Admin enforcement exists, but (1) `/list` and `/unsubscribe` silently reject perfectly valid admins who haven't interacted with the bot before, and (2) the generic message handler replies to every non-YouTube group message, effectively spamming shared chats once privacy mode is disabled. These issues block AC5/AC6/AC9.

### Detailed Findings

1. **Blocking – Fresh admins cannot manage existing chat subscriptions (AC5 & AC6)**  
   `_get_chat_and_user` refuses to create or even look up the chat whenever the acting admin lacks a prior `users` row (`src/bot/handlers.py:246-260`). Both `/list` and `/unsubscribe` immediately bail when that happens and tell the admin they have no subscriptions (`src/bot/handlers.py:264-301`, `src/bot/handlers.py:303-356`). Any secondary admin (or a newly promoted owner) therefore cannot audit or clean up the chat’s subscriptions without first DM-ing the bot to create a user record, which violates the requirement that these commands be available to all administrators.
2. **Blocking – Bot spams every non-YouTube group message (AC9)**  
   `handle_message` replies with “Send me a YouTube URL…” for *every* text update that doesn’t contain a YouTube link (`src/bot/handlers.py:591-625`). Because Story 1.9 relies on disabling Telegram’s privacy mode so that the bot can see group messages, this logic will cause the bot to respond to virtually every conversation in a group or channel, drowning users in noise and making the feature unusable in production. At minimum the handler must ignore non-YouTube traffic (or restrict this behaviour to private chats).

### Acceptance Criteria Validation

| AC | Status | Notes |
|----|--------|-------|
| 1 | ✅ Met | `_ensure_chat_record` normalizes chat type/title for every handler (`src/bot/handlers.py:103-157`). |
| 2 | ✅ Met | `_require_admin` defers to `ACLService` for shared contexts before any sensitive work (`src/bot/handlers.py:118-175`). |
| 3 | ✅ Met | Chat-aware ORM model plus migration and repositories satisfy schema refactor (`src/database/models.py:13-90`, `src/database/migrations/versions/20241202_implement_chat_support.py`). |
| 4 | ✅ Met | `/subscribe` always routes through `_require_admin` and persists chat contexts (`src/bot/handlers.py:231-245`, `src/bot/handlers.py:439-585`). |
| 5 | ❌ Fail | `/list` stops with “no subscriptions” whenever the acting admin has no `users` row, even if the chat has subscriptions (`src/bot/handlers.py:264-301`). |
| 6 | ❌ Fail | `/unsubscribe` (and its callback) share the same dependency on a pre-existing user record, so additional admins cannot remove subscriptions (`src/bot/handlers.py:303-420`). |
| 7 | ✅ Met | Webhook + notification pipeline sends chat-scoped updates with discussion prompts (`src/webhooks/handlers.py:64-151`, `src/bot/notifications.py:9-78`). |
| 8 | ✅ Met | Group/channel notifications append the required discussion guidance (`src/bot/notifications.py:28-74`). |
| 9 | ❌ Fail | Generic text handler responds to *all* non-YouTube messages, which is incompatible with group privacy-mode requirements (`src/bot/handlers.py:591-641`). |
| 10 | ✅ Met | Alembic upgrade/downgrade handle schema migration with reversible steps (`src/database/migrations/versions/20241202_implement_chat_support.py:60-213`). |
| 11 | ✅ Met | CLAUDE.md documents rollout/migration steps for operators (`CLAUDE.md:216-255`). |
| 12 | ✅ Met | Full `pytest` + mypy suites execute cleanly (`tests/integration/test_bot_flow.py`, `tests/unit/test_repository.py`, `tests/unit/test_handlers.py`). |

### Validation Notes

- Tests executed: `uv run pytest`
