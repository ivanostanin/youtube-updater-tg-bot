# Requirements Traceability Matrix

## Story: 1.4 - Configure GitHub Actions CI/CD with Allure Reports

### Coverage Summary

- Total Requirements: 8
- Fully Covered: 8 (100%)
- Partially Covered: 0 (0%)
- Not Covered: 0 (0%)

### Requirement Mappings

#### AC1: Create `.github/workflows/test.yml` workflow file

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::tests job`
    - Given: A commit on `main` or a pull request is opened
    - When: GitHub Actions loads the `CI` workflow definition
    - Then: The workflow initializes with declared concurrency and required permissions, ensuring the pipeline file exists and is executable
    - Coverage: integration

#### AC2: Workflow runs on push to main and all pull requests

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::on triggers`
    - Given: Either a push to `main` or any pull request event occurs
    - When: GitHub Actions evaluates the `on` configuration
    - Then: The CI workflow is scheduled for execution, validating trigger coverage for both event types
    - Coverage: integration

#### AC3: Workflow installs dependencies and runs pytest with Allure

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::tests job`
    - Given: Runner provisions Python 3.13 with cached dependencies
    - When: Steps execute `pip install -e ".[dev]"`, `ruff check`, `mypy src`, and `pytest --alluredir=allure-results`
    - Then: Quality gates and pytest run with Allure result generation, blocking on failures
    - Coverage: integration

#### AC4: Workflow generates Allure HTML report

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::Generate Allure HTML report`
    - Given: `allure-results` directory is available after pytest
    - When: The workflow step invokes `allure generate allure-results --clean -o allure-report`
    - Then: Rendered HTML artifacts are produced for downstream publishing
    - Coverage: integration

#### AC5: Workflow uploads Allure report as GitHub Actions artifact (30-day retention)

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::Upload Allure HTML report`
    - Given: The `allure-report` directory contains generated HTML files
    - When: `actions/upload-artifact@v4` runs with `retention-days: 30`
    - Then: The rendered Allure report is retained as a downloadable artifact for 30 days
    - Coverage: integration

#### AC6: Configure GitHub Pages deployment for Allure reports

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::Upload Pages artifact`
    - Given: Publish bundle prepared under `publish/allure-latest/`
    - When: Workflow uploads the Pages artifact during non-PR events
    - Then: Deployment inputs are staged for the Pages workflow
    - Coverage: integration
  - `.github/workflows/test.yml::deploy job`
    - Given: Tests job completes successfully on `main`
    - When: `actions/deploy-pages@v4` executes with `pages: write` permissions
    - Then: Allure HTML is published to `https://ivanostanin.github.io/youtube-updater-tg-bot/allure-latest/`
    - Coverage: integration

#### AC7: Add status badge to README.md showing test pass/fail status

- **Coverage:** full
- **Test Mappings:**
  - `README.md::CI badge`
    - Given: Rendered README header with badge markdown
    - When: GitHub fetches `https://github.com/ivanostanin/youtube-updater-tg-bot/actions/workflows/test.yml/badge.svg?branch=main`
    - Then: Badge reflects current CI status for visibility on repository homepage
    - Coverage: documentation

#### AC8: Workflow fails PR if tests fail (blocks merge)

- **Coverage:** full
- **Test Mappings:**
  - `.github/workflows/test.yml::tests job`
    - Given: Lint or pytest encounters a failure
    - When: Step returns non-zero exit status
    - Then: Job terminates the workflow without `continue-on-error`, preventing PR merges when checks fail
    - Coverage: integration
  - `.github/workflows/test.yml::deploy job`
    - Given: Tests job fails or does not succeed
    - When: `needs: tests` is evaluated
    - Then: Deployment job is skipped, ensuring no artifacts publish on failed test runs
    - Coverage: integration

### Coverage Gaps

No gaps identified; all acceptance criteria have active enforcement through the CI workflow.

### Test Design Recommendations

1. Add a nightly monitoring check that validates the published Pages URL renders the latest report contents to catch caching regressions.
2. Introduce an automated verification (e.g., using `act` or lightweight smoke scripts) to ensure badge URLs and artifact retention settings remain correct after dependency bumps.

### Risk Assessment

- **High Risk:** None – All criteria are enforced by CI runtime behavior.
- **Medium Risk:** Pages deployment relies on GitHub infrastructure; monitor initial publishes.
- **Low Risk:** README badge and artifact uploads provide quick visibility and evidence retention.
